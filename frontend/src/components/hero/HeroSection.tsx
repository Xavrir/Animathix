"use client";

// HeroSection is no longer used as a standalone component.
// The hero is now integrated directly into the landing page (app/page.tsx).
// Keeping this file for potential reuse.

export default function HeroSection() {
  return null;
}
